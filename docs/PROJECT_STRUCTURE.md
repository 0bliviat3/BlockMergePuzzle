# Block Merge Puzzle - 프로젝트 구조

## 📁 전체 디렉토리 구조

```
BlockMergePuzzle/
│
├── Assets/                         # Unity 에셋 폴더
│   ├── Scenes/                     # 씬 파일들
│   │   ├── MainMenu.unity          # 메인 메뉴 씬
│   │   ├── GameScene.unity         # 메인 게임 씬
│   │   └── SettingsScene.unity     # 설정 씬 (옵션)
│   │
│   ├── Scripts/                    # C# 스크립트들
│   │   ├── Core/                   # 핵심 게임 로직
│   │   │   ├── Block.cs            # 블록 클래스
│   │   │   ├── Grid.cs             # 그리드 관리
│   │   │   ├── BlockMerger.cs      # 병합 로직
│   │   │   └── GameManager.cs      # 게임 전체 관리
│   │   │
│   │   ├── UI/                     # UI 관련
│   │   │   ├── ScoreManager.cs     # 점수 UI
│   │   │   └── UIController.cs     # UI 전체 관리 (추가 예정)
│   │   │
│   │   ├── Input/                  # 입력 처리
│   │   │   └── InputHandler.cs     # 터치/입력 처리
│   │   │
│   │   ├── Effects/                # 이펙트
│   │   │   └── EffectManager.cs    # 이펙트 관리
│   │   │
│   │   ├── Audio/                  # 오디오
│   │   │   └── AudioManager.cs     # 오디오 관리
│   │   │
│   │   ├── System/                 # 시스템
│   │   │   ├── SaveManager.cs      # 저장/로드
│   │   │   ├── AchievementManager.cs # 업적
│   │   │   └── GameConfig.cs       # 게임 설정
│   │   │
│   │   └── Utilities/              # 유틸리티
│   │       └── LeanTween.cs        # 애니메이션 라이브러리
│   │
│   ├── Prefabs/                    # 프리팹들
│   │   ├── Gameplay/               # 게임플레이 프리팹
│   │   │   ├── Block.prefab        # 블록 프리팹
│   │   │   └── Cell.prefab         # 셀 프리팹
│   │   │
│   │   ├── UI/                     # UI 프리팹
│   │   │   ├── ScorePanel.prefab   # 점수 패널
│   │   │   ├── GameOverPanel.prefab # 게임오버 패널
│   │   │   └── ComboPanel.prefab   # 콤보 패널
│   │   │
│   │   └── Effects/                # 이펙트 프리팹
│   │       ├── MergeEffect.prefab  # 병합 이펙트
│   │       ├── ExplodeEffect.prefab # 폭발 이펙트
│   │       └── ParticleEffect.prefab # 파티클 이펙트
│   │
│   ├── Materials/                  # 머티리얼
│   │   ├── BlockMaterial.mat       # 블록 머티리얼
│   │   └── ParticleMaterial.mat    # 파티클 머티리얼
│   │
│   ├── Sprites/                    # 스프라이트/이미지
│   │   ├── UI/                     # UI 스프라이트
│   │   │   ├── Icons/              # 아이콘들
│   │   │   ├── Buttons/            # 버튼 이미지
│   │   │   └── Backgrounds/        # 배경 이미지
│   │   │
│   │   ├── Blocks/                 # 블록 이미지
│   │   └── Effects/                # 이펙트 텍스처
│   │
│   ├── Audio/                      # 오디오 파일
│   │   ├── Music/                  # 배경음악
│   │   │   ├── menu_bgm.mp3        # 메뉴 음악
│   │   │   └── game_bgm.mp3        # 게임 음악
│   │   │
│   │   └── SFX/                    # 효과음
│   │       ├── merge.wav           # 병합 사운드
│   │       ├── explode.wav         # 폭발 사운드
│   │       ├── click.wav           # 클릭 사운드
│   │       └── combo.wav           # 콤보 사운드
│   │
│   ├── Fonts/                      # 폰트
│   │   └── GameFont.ttf            # 게임 폰트
│   │
│   └── Resources/                  # 런타임 로드 리소스
│       └── Config/                 # 설정 파일
│
├── ProjectSettings/                # Unity 프로젝트 설정
│   ├── ProjectSettings.asset       # 프로젝트 설정
│   ├── TagManager.asset            # 태그/레이어 설정
│   └── QualitySettings.asset       # 품질 설정
│
├── Packages/                       # 패키지 매니페스트
│   └── manifest.json               # 패키지 종속성
│
├── Documentation/                  # 문서 (이 폴더는 Assets 외부)
│   ├── README.md                   # 프로젝트 소개
│   ├── SETUP_GUIDE.md              # 설정 가이드
│   ├── BUILD_GUIDE.md              # 빌드 가이드
│   ├── ROADMAP.md                  # 개발 로드맵
│   └── API_REFERENCE.md            # API 문서 (추가 예정)
│
└── Builds/                         # 빌드 출력 (gitignore)
    ├── Android/                    # Android 빌드
    └── iOS/                        # iOS 빌드
```

## 📋 주요 스크립트 설명

### Core (핵심 게임 로직)

#### Block.cs
```
역할: 개별 블록의 데이터와 동작 관리
주요 기능:
- 블록 초기화
- 레벨 관리
- 색상/외관 업데이트
- 애니메이션 (병합, 폭발, 이동)
- 블록 값 계산
```

#### Grid.cs
```
역할: 게임 그리드 관리
주요 기능:
- 그리드 생성
- 블록 추가/제거/이동
- 위치 계산
- 빈 공간 검색
- 인접 블록 검색
- 그리드 상태 확인
```

#### BlockMerger.cs
```
역할: 블록 병합 로직
주요 기능:
- 블록 선택/해제
- 병합 가능 여부 확인
- 병합 실행
- 폭발 처리
- 연쇄 반응 체크
- 자동 병합 (AI 힌트)
```

#### GameManager.cs
```
역할: 게임 전체 흐름 관리
주요 기능:
- 게임 시작/종료/재시작
- 새 블록 추가
- 게임 오버 체크
- 최고 레벨 추적
- 힌트 기능
- 일시정지/재개
```

### UI (사용자 인터페이스)

#### ScoreManager.cs
```
역할: 점수 및 콤보 관리
주요 기능:
- 점수 추가/계산
- 콤보 시스템
- 최고 점수 관리
- UI 업데이트
- 애니메이션
```

### Input (입력 처리)

#### InputHandler.cs
```
역할: 사용자 입력 처리
주요 기능:
- 터치/마우스 입력 감지
- UI 필터링
- 스와이프 제스처 (확장용)
```

### Effects (이펙트)

#### EffectManager.cs
```
역할: 게임 이펙트 관리
주요 기능:
- 병합 이펙트
- 폭발 이펙트
- 힌트 이펙트
- 마일스톤 이펙트
- 화면 효과 (흔들림, 플래시)
- 사운드 재생
```

### Audio (오디오)

#### AudioManager.cs
```
역할: 오디오 시스템 관리
주요 기능:
- 배경음악 재생
- 효과음 재생
- 볼륨 조절
- 음소거 기능
- 페이드 인/아웃
- 설정 저장/로드
```

### System (시스템)

#### SaveManager.cs
```
역할: 데이터 저장/로드
주요 기능:
- 게임 저장
- 게임 로드
- 통계 저장
- 데이터 삭제
```

#### AchievementManager.cs
```
역할: 업적 시스템
주요 기능:
- 업적 초기화
- 업적 체크
- 업적 해제
- 진행률 계산
- 알림 표시
```

#### GameConfig.cs
```
역할: 게임 설정 및 상수 관리
주요 기능:
- 게임 상수 정의
- 난이도 설정
- 색상 계산
- 점수 계산
```

## 🎨 에셋 구성

### Prefabs (프리팹)
```
Block.prefab
- Image (블록 배경)
- TextMeshPro (레벨 텍스트)
- BoxCollider2D (터치 감지)
- Block.cs (스크립트)

Cell.prefab
- Image (셀 배경)
- 반투명 회색 배경
```

### Sprites (스프라이트)
```
필요한 이미지:
- 앱 아이콘 (1024x1024)
- 블록 배경
- 버튼 UI
- 배경 이미지
- 파티클 텍스처
```

### Audio (오디오)
```
Music:
- menu_bgm.mp3 (메뉴 음악)
- game_bgm.mp3 (게임 음악)

SFX:
- merge.wav (병합)
- explode.wav (폭발)
- click.wav (클릭)
- combo.wav (콤보)
- achievement.wav (업적)
- gameover.wav (게임오버)
```

## 🔧 Unity 설정

### Tags
```
- Block
- Cell
```

### Layers
```
Layer 8: BlockLayer (블록 레이어)
```

### Quality Settings
```
Texture Quality: Medium
Anti Aliasing: 2x Multi Sampling
VSync Count: Don't Sync
```

### Physics 2D
```
Gravity: (0, 0)
```

### Input System
```
Legacy Input (Default)
```

## 📦 필수 패키지

### Unity Packages
```
- TextMeshPro (필수)
- 2D Sprite (포함)
- Unity UI (포함)
```

### Third-Party Assets
```
- LeanTween (애니메이션)
```

## 🌐 네임스페이스 구조

```csharp
// 코어 게임플레이
namespace BlockMerge.Core
{
    Block.cs
    Grid.cs
    BlockMerger.cs
    GameManager.cs
}

// UI 시스템
namespace BlockMerge.UI
{
    ScoreManager.cs
    UIController.cs
}

// 시스템
namespace BlockMerge.System
{
    SaveManager.cs
    AchievementManager.cs
    GameConfig.cs
}

// 유틸리티
namespace BlockMerge.Utilities
{
    // 헬퍼 클래스들
}
```

## 📊 데이터 흐름

```
User Input → InputHandler
    ↓
BlockMerger (선택/병합 로직)
    ↓
Grid (블록 상태 업데이트)
    ↓
Block (개별 블록 애니메이션)
    ↓
ScoreManager (점수 업데이트)
    ↓
EffectManager (이펙트 재생)
    ↓
GameManager (게임 상태 체크)
```

## 🔄 게임 루프

```
게임 시작
    ↓
그리드 초기화
    ↓
초기 블록 생성
    ↓
┌─────────────────┐
│  메인 루프       │
│  1. 입력 대기    │
│  2. 블록 병합    │
│  3. 애니메이션   │
│  4. 점수 업데이트 │
│  5. 새 블록 추가  │
│  6. 게임오버 체크 │
└─────────────────┘
    ↓
게임 종료
```

## 📝 코딩 컨벤션

### 네이밍
```csharp
// 클래스: PascalCase
public class GameManager

// 메서드: PascalCase
public void StartGame()

// 변수: camelCase
private int currentScore;

// 상수: UPPER_SNAKE_CASE
private const int MAX_GRID_SIZE = 10;

// 프로퍼티: PascalCase
public int CurrentScore { get; set; }
```

### 주석
```csharp
/// <summary>
/// 메서드 설명
/// </summary>
/// <param name="parameter">파라미터 설명</param>
/// <returns>반환값 설명</returns>
public int MethodName(int parameter)
{
    // 단일 라인 주석
    return parameter;
}
```

## 🎯 개발 우선순위

### Phase 1: 핵심 기능 (완료)
- ✅ 그리드 시스템
- ✅ 블록 병합
- ✅ 점수 시스템
- ✅ 기본 UI

### Phase 2: 폴리싱
- ⬜ 파티클 이펙트
- ⬜ 사운드 강화
- ⬜ UI/UX 개선
- ⬜ 튜토리얼

### Phase 3: 확장
- ⬜ 게임 모드 추가
- ⬜ 아이템 시스템
- ⬜ 소셜 기능

## 📖 참고 자료

### Unity 문서
- [Unity Manual](https://docs.unity3d.com/)
- [C# Scripting Reference](https://docs.unity3d.com/ScriptReference/)
- [TextMeshPro](https://docs.unity3d.com/Packages/com.unity.textmeshpro@3.0/manual/)

### 외부 라이브러리
- [LeanTween](http://dentedpixel.com/LeanTweenDocumentation/)

---

**Project Structure Last Updated: 2024**
